---
title: Bases de Datos (MySQL & MongoDB)
description: Instalación y mantenimiento de motores de base de datos para CSDOCS.
---

import { Steps, Tabs, TabItem, Card } from '@astrojs/starlight/components';

# 🗄️ Bases de Datos

CSDOCS utiliza una arquitectura híbrida con **MySQL** para datos relacionales y **MongoDB** para almacenamiento de documentos y metadatos flexibles.

---

## 🐬 MySQL 8.0

### Instalación en Linux
```bash
sudo apt install mysql-server -y
sudo mysql_secure_installation
```

### Configuración Recomendada
Edita `/etc/mysql/mysql.conf.d/mysqld.cnf` para optimizar el rendimiento:
```ini
innodb_buffer_pool_size = 1G  # Ajustar al 50-70% de la RAM disponible
max_connections = 500
```

---

## 🍃 MongoDB 7.0

### Instalación
```bash
sudo apt-get install gnupg curl
curl -fsSL https://www.mongodb.org/static/pgp/server-7.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-7.0.gpg --dearmor
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-7.0.gpg ] https://repo.mongodb.org/apt/ubuntu noble/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list
sudo apt-get update
sudo apt-get install -y mongodb-org
```

### Comandos de Operación
```bash
sudo systemctl start mongod
sudo systemctl enable mongod
# Verificar estado
sudo systemctl status mongod
```

---

## 🛡️ Respaldo de Datos (Backups)

### MySQL Dump
```bash
mysqldump -u [usuario] -p [base_de_datos] > backup.sql
```

### Mongo Dump
```bash
mongodump --db [nombre_db] --out /ruta/backup/
```

<Card title="Seguridad" icon="lock">
  Nunca expongas los puertos 3306 (MySQL) o 27017 (MongoDB) a internet sin restricciones de IP en el firewall.
</Card>
